import { StatusMessages } from './statusMessages';\n\nexport class BotController {\n\n  public postPlanningStarted(): string {\n    const message = StatusMessages.planningStarted();\n    return message;\n  }\n\n  public postPlanningCompleted(): string {\n    const message = StatusMessages.planningCompleted();\n    return message;\n  }\n\n  public postPlanningBlocked(reason: string): string {\n    const message = StatusMessages.planningBlocked(reason);\n    return message;\n  }\n\n  public postImplementationStarted(branchName: string): string {\n    const message = StatusMessages.implementationStarted(branchName);\n    return message;\n  }\n\n  public postImplementationCompleted(prLink: string): string {\n    const message = StatusMessages.implementationCompleted(prLink);\n    return message;\n  }\n\n  public postImplementationFailed(error: string): string {\n    const message = StatusMessages.implementationFailed(error);\n    return message;\n  }\n\n  public postReworkStarted(): string {\n    const message = StatusMessages.reworkStarted();\n    return message;\n  }\n\n  public postReworkCompleted(): string {\n    const message = StatusMessages.reworkCompleted();\n    return message;\n  }\n\n  public postReworkFailed(error: string): string {\n    const message = StatusMessages.reworkFailed(error);\n    return message;\n  }\n\n}\n