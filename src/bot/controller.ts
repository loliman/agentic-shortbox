import { StatusMessages } from './statusMessages';\n\nexport class BotController {\n\n  // Example of how to use the consolidated messages\n  public postPlanningStarted(): string {\n    return StatusMessages.planningStarted();\n  }\n\n  public postPlanningCompleted(): string {\n    return StatusMessages.planningCompleted();\n  }\n\n  public postPlanningBlocked(reason: string): string {\n    return StatusMessages.planningBlocked(reason);\n  }\n\n  public postImplementationStarted(branchName: string): string {\n    return StatusMessages.implementationStarted(branchName);\n  }\n\n  public postImplementationCompleted(prLink: string): string {\n    return StatusMessages.implementationCompleted(prLink);\n  }\n\n  public postImplementationFailed(error: string): string {\n    return StatusMessages.implementationFailed(error);\n  }\n\n  public postReworkStarted(): string {\n    return StatusMessages.reworkStarted();\n  }\n\n  public postReworkCompleted(): string {\n    return StatusMessages.reworkCompleted();\n  }\n\n  public postReworkFailed(error: string): string {\n    return StatusMessages.reworkFailed(error);\n  }\n\n}\n
