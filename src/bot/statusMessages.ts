export const StatusMessages = {\n  planningStarted: () => {\n    return '🚀 Planning phase has begun! The bot is analyzing the issue to create an actionable implementation plan.';\n  },\n  planningCompleted: () => {\n    return '✅ Planning phase completed. Review the implementation plan and proceed with `ready to implement` when satisfied.';\n  },\n  planningBlocked: (reason: string) => {\n    return `⚠️ Planning phase is blocked: ${reason} Please clarify or adjust the issue to proceed.`;\n  },\n  implementationStarted: (branchName: string) => {\n    return `🔨 Implementation has started on branch \`${branchName}\`. The bot is working on the defined tasks.`;\n  },\n  implementationCompleted: (prLink: string) => {\n    return `✅ Feature implementation is complete! A pull request has been created: ${prLink}`;\n  },\n  implementationFailed: (error: string) => {\n    return `❌ Implementation failed due to an error: ${error}. Please review and re-trigger if needed.`;\n  },\n  reworkStarted: () => {\n    return '🔄 Rework phase has begun based on the review feedback. The bot is addressing the requested changes.';\n  },\n  reworkCompleted: () => {\n    return '✅ Rework phase completed! All requested changes have been applied.';\n  },\n  reworkFailed: (error: string) => {\n    return `❌ Rework failed due to an error: ${error}. Please review and re-trigger if necessary.`;\n  }\n};\n